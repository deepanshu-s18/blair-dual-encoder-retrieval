# BLaIR-style Dual Encoder Product Retrieval
# Requirements for Kaggle T4 / Google Colab (Python 3.10+)

# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.0.0                   # CUDA 11.8 on Kaggle; CPU ok for inference
transformers>=4.38.0           # BERT tokenizer + BertModel
datasets>=2.18.0               # Amazon Reviews 2023 via HuggingFace Hub

# ── Retrieval ─────────────────────────────────────────────────────────────────
faiss-cpu>=1.7.4               # Exact inner-product search (FAISS IndexFlatIP)
                               # On GPU Kaggle, install faiss-gpu instead
rank_bm25>=0.2.2               # BM25Okapi for lexical baseline + hard negatives

# ── Data processing ───────────────────────────────────────────────────────────
pandas>=2.1.0
pyarrow>=14.0.0               # parquet I/O (fast columnar storage)
numpy>=1.24.0

# ── Cross-encoder reranker ────────────────────────────────────────────────────
sentence-transformers>=2.6.0   # cross-encoder/ms-marco-MiniLM-L-6-v2

# ── Statistics ────────────────────────────────────────────────────────────────
scipy>=1.11.0                  # chi2.cdf for McNemar's test
                               # NOTE: scipy.stats.mcnemar removed in v1.14 —
                               # use manual chi2 as in run_mcnemar.py

# ── Visualization (notebooks only) ───────────────────────────────────────────
matplotlib>=3.7.0
scikit-learn>=1.3.0            # TSNE, CountVectorizer (error analysis)
tqdm>=4.66.0                   # progress bars
ipykernel>=6.0.0               # Jupyter kernel

# ── Installation notes ────────────────────────────────────────────────────────
# Standard:  pip install -r requirements.txt
# Kaggle T4:
#   !pip install faiss-gpu   # instead of faiss-cpu — 5–10x faster indexing
#   !pip install -r requirements.txt --quiet
#
# GPU memory budget:
#   BiEncoder  (1x BERT, 110M params):  ~2.0 GB with fp16 + batch=16
#   DualEncoder (2x BERT, 220M params): ~3.5 GB with fp16 + batch=16
#   Corpus encode (batch=8):            ~1.5 GB peak
#   FAISS IndexFlatIP (6k × 768):       ~18 MB — negligible
#   → Total peak: ~5 GB / 16 GB T4 VRAM — comfortable margin
